<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 设置背景颜色，可以根据需求修改 -->
    <solid android:color="@color/white" />

    <!-- 设置圆角半径 -->
    <corners android:radius="16dp" />

    <!-- 可选，设置边框 -->
    <stroke
        android:width="2dp"
        android:color="@color/white" /> <!-- 边框颜色 -->
</shape>
